#!/bin/bash

# Firefox doesn't support dynamic theme color via policies.json like Chromium does.
# This script is now a placeholder for future Firefox policy management.
# The directory /etc/firefox/policies is created and writable by config/theme.sh.

exit 0
